# 项目技术上下文

## Dependencies

- **jszip** — 浏览器端解压用户上传的构建产物 / 源码 zip（解压与入口识别全在前端完成，云函数不参与文件处理）
- **base64-arraybuffer** — Meoo Cloud Storage 上传要求 ArrayBuffer 载荷，用其 `decode` 把 base64 转成 ArrayBuffer
- **three** — 页面级 3D 星海背景：`components/ThreeBackground.tsx`（薄壳 + 黄昏底）、`lib/three-scene.ts`（星屑/粉尘/星云/流星 + 滚动穿越）、`lib/three-shaders.ts`（GLSL）、`lib/sky-dome.ts`（天穹层：月亮 / 卫星过境 / 低空云带）、`lib/bg3d.ts`（唯一开关，localStorage `pf-bg3d`）。用 `import("three")` 动态引入（构建成独立 chunk，不进首屏），类型来自 devDependency `@types/three`（three 自身不发 `.d.ts`）
- **Google Fonts（CDN link）** — Baloo 2（标题）/ Noto Sans SC（正文）/ JetBrains Mono（代码感点缀），在 `index.html` 引入

## Architecture

- 单页站点（`src/routes/index.tsx`），全站状态集中在 `src/lib/site-context.tsx` 的 `SiteProvider`：站点设置、项目列表、编辑模式、设置抽屉开关都在此，子组件通过 `useSite()` 消费。
- 数据分两层：`site_settings`（单行，`id=1` + CHECK 约束）存资料与背景配置；`projects` 存项目。文件全部落在 `portfolio` 存储桶，表里只存 public URL 与文件清单 JSON。
- 背景定制以 JSON 字段 `backgrounds` 按区块 key 存储，`SectionBackground` 统一渲染「图片 / 内置渐变 / 跟随整站」三种模式并叠加柔光遮罩，`DEFAULT_BACKGROUNDS` 是唯一默认值来源。
- **3D 背景层与区块背景是叠加关系，不是替换关系（勿改成替换）**：`ThreeBackground` 是**页面级唯一**的 canvas，`fixed inset-0 z-[1]` —— 故意压在区块背景（`z-auto`）之上、正文（`z-10`）与弹层（`z-50` 起）之下，所以既不会遮住任何既有图片 / 渐变背景，也不会压到正文和弹窗，**不需要**在 `BackgroundConfig` 里加新模式、也不改 `SectionBackground`。全站唯一的开关是 `lib/bg3d.ts`（localStorage `pf-bg3d`，默认开，设置抽屉「背景」页有开关）。之所以敢做成常开：动态加载失败、WebGL 不可用、上下文丢失都会只移除自己，页面立刻回退到 CSS 炫彩渐变；DPR 上限 1.5（移动端）/ 2（桌面）、移动端降载、`document.hidden` 停绘、帧率上限 30/45、`prefers-reduced-motion` 只画一帧静图。
- **3D 星海的三条勿回退约束**：① **纵深回绕必须在着色器里做**（`lib/three-shaders.ts` 里的 `mod` + 每帧只推一个 `uTravel`）——一旦改成逐帧改顶点或重建几何体，星屑一加密就掉帧。② **回绕点必须落在视野外**（相机后方 + 远平面附近），两端靠 `vFade` / `vDepthFade` 淡出——回绕点进到可见范围就会看到"啪"地闪现。③ **推进量只由滚动位置决定**（`lib/three-scene.ts` 的 `readScroll()` 按可滚动范围归一化并夹紧），禁止改成时间累积式单向飞行，否则滑回原位画面回不来、滑到底还在飞。另有两条配套取舍：星屑/光尘用**加色混合**（星是光源，要「亮过底」；普通混合在压暗的底上也会被读成暗点），星云 / 低云同样加色；CPU 侧只更新十几个对象（流星 + 天穹层），这个数量级不要涨。
- **星点尺寸只由星等决定，绝不随纵深放大（勿回退）**：真实恒星是未分辨点（肉眼分辨率极限约 1′，最大的参宿四也只有 0.04″），一旦按透视 `aSize*uScale/d` 算尺寸，近处星会被撑成 20~50px 的光斑 —— 这就是用户两轮反馈「太模糊」「不像星空」的共同根因。现在 `aSize` 直接就是 CSS 像素直径，着色器里只乘 `uPx`（设备像素比），JS 侧用幂律 `pow(rand,2.4)` 把分布压到 1.2~5.2px，`gl_PointSize` 上限 26 设备像素。
- **星要「亮」，底就必须暗（勿回退）**：sRGB 的白就是显示上限，加色白点落在近白底上不可能亮过纯白，只会被读成黑点或彩色纸屑 —— 浅底与真实星空本身互斥。解法是 `ThreeBackground.tsx` 里那层 `mix-blend-mode: multiply` 的夜色渐变：multiply 按比例压暗，站长上传的背景图仍然透得出，只是从白昼变成夜里。**这层底铺满整个视口**，全站都有星；把它收进首屏换来的就是「只有首页有星空」，用户明确否掉过那个版本。
- **夜色底的两个坑（勿回退）**：① multiply 层自己必须就是那个 `fixed z-[1]` 元素，**不能**再套一层带 z-index 的容器 —— 父级一旦建立 stacking context，blend 的 backdrop 就被限制在父级内部，而父级是透明的，multiply 会静默失效（表现是页面像盖了块不透明蓝布）。② 亮度余量是算出来的：全屏幕最亮一档合成后相对亮度 ≤0.12，`--night-*` 浅字才有 4.5:1（历史教训：顶部暗带曾把 tagline 压到 1.69:1）。所以分工固定为 —— 直接压在天空上的字（首屏标题/导语、区块标题、页脚、未滚动的导航）用 `--night-ink` / `--night-ink-soft` / `--night-label`；自带浅色底的卡片与胶囊仍用深色 `--ink`。为此 `--card` 的不透明度提到 86%、`.glass-card` 到 84%，否则玻璃卡会被夜色拖穿。
- **星野的颜色与闪烁**：93% 的星是纯白（暗光下视杆细胞不辨色），只有最亮约 7% 允许带蓝白/暖黄偏。四芒光针是望远镜副镜、相机叶片和眼球晶状体的成像伪影，不是星本身的形态 —— 只给屏幕 ≥7 设备像素的极少数星，长度 ≤0.5 sprite 半径。闪烁只让 40% 的星参与、各自独立相位与 0.2~1.2Hz 频率，幅度 ∝ (1/sin 仰角)^1.5（越靠地平线大气路径越长越闪），另设 3 颗完全不闪的「行星」：面源会把抖动平均掉，这正是行星不眨眼的原因。
- **天穹层从属于星野（勿回退）**：月亮 / 卫星 / 低云 / 远处天体在 `lib/sky-dome.ts`，挂在**相机空间**（视为无限远），只跟指针做 1.2% 反向位移，不参与 `travel` 的纵深穿越。极光带与线框多面体已停用（`createRibbons()` / `createShapes()` 调用注释保留以便回退）—— 14 颗 0.8 不透明度的线框几何体会把画面主体从星野抢走，用户原话就是「像多边体」。真实夜空里没有漂浮几何体。
- **月亮要「在走」，而且不能像贴纸（勿回退）**：月亮单独放在 `moonArm` 分组，**不参与 wheel 轮转** —— 挂在轮转组里它的位移是绕屏幕中心画圆，滚一整页才偏 16°，观感就是钉死不动（用户原话「那个月球非常突兀，一动不动」）。现在由 `ang = MOON_ANG0 + travel*MOON_TRAVEL + elapsed*MOON_IDLE` 沿 sin/cos 弧缓慢横越天空：滚一屏 ≈ 30°，不滚也以约 4% 屏宽/分钟的量级继续爬，用 sin/cos 而不是线性累加，角度越界也不会跳回或飘出画面。形态上：边缘 `smoothstep` 羽化 + 月面外一层散射，暗面掺 `uSky` 天空色，halo 收到 0.19 视高、`uOpacity` 压到 0.86 —— 硬边纯白圆盘就是「贴图感」的来源。
- 构建产物体验链路：`lib/zip.ts` 解压 → `assembleBundle` 找入口 index.html → 资源先上传得到 `path→url` 映射 → `rewriteEntryHtml` 用 DOMParser 改写相对引用并注入 `<base>` → 改写后的 HTML 以 `text/html` 上传作为 iframe src。因此体验 URL 是 storage 上的静态文件，不依赖本站路由。GitHub 同步项目的体验走另一条路：sync 探测仓库 HTML 入口存入 `projects.gh_entry_path`；≤2MB 时由 `mirrorDemoHtml()` 自动镜像进桶（`gh-demo/{owner}/{repo}/{ref}/{file}`）并把桶内 path 写入 `projects.entry_url`，超大文件跳过自动镜像、留给站长在详情页「上传 index.html」（复用 `uploadSingleHtml`）。前端统一 `fetchMirroredHtml(entry)` 拉文本 → 交给 `iframe srcDoc`，**绝不能 iframe 直挂 storage URL**。仓库 raw 只在站内版本读不到时兜底（`githubRawUrl`，注入 `<base>` 让相对资源按仓库目录解析，6s 超时）——它**永远是第二顺位**：仓库里的文件可能根本跑不起来（408- 的 `app/index.html` 题库脚本就是坏的），把它排在前面等于让用户直接看一份坏版本。
- 设置抽屉与作品区之间用 `window` 自定义事件（`pf:new-project` / `pf:edit-project`）传递意图，避免跨组件层传回调。
- 体验页「放大」= 在**当前页面**把 iframe 铺满整屏（详情弹窗切成 `h-[100svh] max-w-none rounded-none`，标题/标签/简介/选项卡全部收起，只留右上角一组悬浮的「退出放大 / 关闭」，Esc 退回弹窗），**不是新标签页、也不换路由**。加载用绝对定位遮罩 + 百分比进度条盖住 iframe，`iframe.onLoad` 触发后才收掉遮罩，避免半成品页面先露出来。**体验页加载顺序：站内镜像优先、仓库 raw 兜底**（raw 在访问不到 GitHub 的环境要干等 12s 超时，用户会以为「打不开」）；站长手动传过体验页（`entry_url` 进 `overridden_fields`）时**固定**站内优先，且跳过「快照是否落后」的体积比对。
- **GitHub 同步项目的编辑与「改过即锁定」**：手动项目与同步项目共用同一个 `ProjectEditor`，体验内容区抽成 `DemoContentEditor`（`variant="full"` 给手动项目、`"github"` 只留单文件/外链）。站长在编辑器里改动过的字段名会并入 `projects.overridden_fields`（并集合并，不覆盖上一次），`github-sync` 构造 payload 后按 `LOCKABLE = title/description/tags/hidden` 白名单 `delete payload[key]`，`entry_url` / `demo_mode` 同理；`gh_*`、`default_branch`、`readme_path_hint` 等派生信息仍随仓库刷新。改「简介」时同时写 `github_repos.intro_zh` 并置 `intro_source='manual'`，保持设置抽屉里两处文案一致。
- **`github-sync` 的性能契约（勿回退，线上 504 就是破在这上面）**：① 每个仓库的 README / HTML 入口探测只发 **1 次** `git/trees?recursive=1`，从同一棵树派生 `readme_path` + `demo_entry_path`；② `github_repos.probed_pushed_at` 是探测水位线，与仓库当前 `pushed_at` 相同就整段跳过探测（仓库没推送过，探测结果不可能变）；③ 全程受 `DEADLINE_MS = 40s` 约束并分批（`BATCH_SIZE = 6`），一轮做不完返回 `{done:false, nextCursor}`，前端 `runGithubSync()` 自动带游标续跑直到 done（最多 12 轮）；④ 剩余预算不足 `MIRROR_RESERVE_MS = 15s` 时不做镜像并把游标停在该仓库上，且**绝不写 `demo_mode`/`entry_url`**（否则体验入口会被临时清空）；⑤ 组织里已消失仓库的清理只在 `done === true` 时执行，分批中途不能删。
- **GitHub 新增仓库自动上架**：`github-sync` 选仓库的口径 = 「`github_repos` 里已勾选展示（`show_on_site`）」∪「库里还没有记录」——后一半让组织里新出现的仓库自动上架；被站长取消勾选的仓库记录还在且 `show_on_site=false`，所以下架持久有效、不会被重新拉起。空仓库（GitHub 报 `size=0`，没有任何提交）排除在外。写 `show_on_site` **只在新建行时写**（`if (!prev) row.show_on_site = true`），upsert 里写成常量会把站长取消勾选的仓库又拉起来。
- **源码工程的开发态入口绝不能当体验页**：`probeRepo()` 先判定仓库是不是源码工程（存在 `vite.config.[cm]?[jt]s` 或 `src/main.(t|j)sx`）——是的话只认 `DEMO_BUILT_NAMES`（`dist` / `build` / `out` / `docs` 下的 `index.html`），根 `index.html` 一律不当入口，一个构建产物都没有就当不可在线体验。镜像前再由 `dependsOnLocalAssets()` 自查（头部引了本地 `<script src>` / `<link rel=stylesheet>` 就搬不走），命中返回 `runnable: false`，调用方连 `gh_entry_path` 一起清空，否则前端会退回仓库 raw 再白屏一次。前端另有一道兜底：`ProjectDetail` 的 `iframe onLoad` 后延时复查 `frameLooksEmpty()`，空白则显示诚实提示 + 「上传 index.html / 看源码 / 去 GitHub」引导。
- **封面统一风格**：`src/lib/cover.ts` 的 `COVER_STYLE` 是唯一美术方向来源（柔光动漫插画、极光渐变、无文字），`buildCoverPrompt()` 按 标题/简介/标签/语言 拼提示词（≤800 字符，qwen-image-2.0 上限）；`generateCover()` → 云函数 `portfolio-cover`（生图后**在函数侧立刻转存**存储桶，因为生图接口只回 ~24h 临时链接且浏览器直连会被 CORS 拦）→ 回写 `projects.cover_url`。编辑模式在 `ProjectEditor`（手动项目）与 `ProjectDetail` 头部（同步项目）各有一个「AI 生成封面」按钮；重新生成时桶内 path 不变，URL 必须带 `?v=` 版本号否则浏览器一直用缓存。
- **`cover_url` 不会被同步冲掉**：它既不在 `github-sync` 的 payload 里，也不在 `LOCKABLE` 白名单里，所以自动同步永远不碰封面。
- 源码在线浏览：`projects.source_files`（jsonb）只存 `{path,size}` 文件树元信息，**不存文件内容**；内容在点击时从 `source_url` 的 zip 懒取，`SourceBrowser` 内 `loadZip` 做模块级缓存（同 url 只解析一次）。README 由 `findReadme` 按层级最浅优先命中，详情页按需拉取。Markdown 渲染为自写零依赖 tokenizer（`lib/markdown.tsx`），输出 React 元素。

## Lessons

- ❌ **「408- 题库不显示」的两次根因（勿回退）**：① 第一次是**版本落后**——站内快照是站长手动上传的旧版，里面没有题库数据；修法是加载前先做一次轻量比对（`mirroredSize(entry)` + `github-proxy` 的 `action=meta`，只回 `size`/`sha`，**绝不回正文**，所以 6.8MB 的大文件也能安全比对），并且编辑模式提供「从仓库拉取最新版存成站内快照」。② 第二次（复发）是**加载顺序被写反**：`if (manualEntry && !mirrorStale) steps.reverse()` 把站长手动上传的版本挤到第二位，浏览器改去加载仓库 `app/index.html`——**而那份文件的题库脚本本身就是坏的**：注入数据时留下了 `const QUESTIONS = []; int turn=0; …`，用 `new Function(body)` 实测直接 `SyntaxError: Unexpected identifier 'turn'`；于是页面框架照常渲染、`QUESTIONS` 未定义，一道题都不显示。修法：删掉 `reverse()`，**站内快照固定第一顺位、仓库 raw 只兜底**；体积比对只对「同步任务自动搬来的快照」生效（手动上传的那份是站长确认跑得起来的版本，不参与比对）。**教训：任何「换一份来源重试」的逻辑，都必须先确认那份来源真的跑得起来——体积小、更新新，都不等于能跑。**
- ℹ️ 平台 storage 对 `HEAD` 请求返回 `content-length: 0`，所以 `mirroredSize()` 在当前实现下实际拿不到体积（比对会落到「信息不足 → 沿用站内快照」分支）。它只是一层保险，不能当主判据；排查 408- 这类问题要用 `cdn.jsdelivr.net` 拉仓库原文 + `new Function()` 做语法体检。
- ❌ **「放大按钮点了没反应」的根因（勿回退）**：`ProjectDetail` 里那个「复位 + 绑定 Esc」的 effect 一度把 `fullscreen` 写进了依赖数组，而它内部又无条件 `setFullscreen(false)` —— 点放大 → `fullscreen` 变 true → effect 因依赖变化重跑 → 立刻把状态打回 false，用户看到的就是点了没有任何反应。修法：复位 effect 只依赖 `project`（负责收起放大态 + 锁 `body.overflow`），Esc 逻辑拆成独立 effect，用 `fullscreenRef` 读最新值，绝不把 `fullscreen` 放进依赖。**任何「自己 set 自己」的复位 effect，都禁止把该状态列进依赖。**
- ❌ **`github-sync` 线上 504 的根因（勿回退）**：早期实现对每个仓库按候选名逐个 `raw` 拉 README（最多 9 次、每次 25s 超时）再做 1 次 tree 探测，GitHub 抖动时单个仓库就能烧掉 200s+，必然撞上平台网关 ~60s 的硬超时——表现为函数最终日志正常（`done total=4`），但前端拿到的是网关返回的 **HTML 504 错误页**，`JSON.parse` 抛出难懂的 `Unexpected token '<'`。修法：探测收敛为 **1 次 `git/trees?recursive=1`** 同时派生 README 路径与 HTML 入口；`github_repos.probed_pushed_at` 做水位线跳过未推送仓库；`DEADLINE_MS = 40s` 时间预算 + `BATCH_SIZE = 6` 分批游标（`done:false + nextCursor`），前端 `runGithubSync()` 自动续跑（≤12 轮）；前端 `callOnce` 把非 JSON 响应转成可读文案并对 5xx 退避重试一次。实测一轮 4 个仓库 **3134ms** 完成。
- 密钥校验走 Edge Function `verify-key`（`-j false` 匿名可调）：前端只提交 `sha256(salt + ':' + 输入)`，明文邮箱与哈希都不进数据库、不出现在前端代码里。改密钥需要重新计算哈希并重新部署函数。
- 预览 iframe 环境下 `crypto.subtle` 可能不可用，`lib/sha256.ts` 带纯 JS 回退实现，否则会静默无法解锁。
- Meoo Cloud 的 bucket id 是 UUID，`supabase.storage.from()` 必须传 UUID 而非 bucket name；本项目常量在 `lib/storage.ts` 的 `BUCKET_ID`。
- Supabase 写操作被 RLS 拦截时不抛错、只返回空数组，所有 INSERT/UPDATE/DELETE 都统一 `.select()` 后判断长度，否则前端会误报保存成功。
- 存储桶 public 只保证可读，匿名上传/删除必须另建 `storage.objects` 的 INSERT/DELETE 策略。
- 文件上传必须在浏览器端完成：Edge Function 上传拿不到公网访问 URL。
- 多文件上传时 `webkitRelativePath` 只在选中文件夹时才有值，单文件回退 `file.name`，否则相对路径全丢导致体验页资源 404。
- ❌ **Blob URL 喂 iframe 会让依赖本地存储的应用静默失效**：`URL.createObjectURL(new Blob([html]))` 生成的 `blob:null/...` origin 恒为 `null`，即使 sandbox 带 `allow-same-origin` 也救不回来，iframe 内 `localStorage` / `IndexedDB` 会被浏览器直接拒绝。408- 的题库就存在本地存储里，表现为界面正常但「题库 0」、题目列表空白。改用 `iframe srcDoc={html}`（继承父页面 origin，存储 API 可用）后恢复正常。**站内 HTML 体验一律用 srcDoc，不要用 Blob URL。**
- ❌ **Edge Function 侧访问 GitHub 不可行**：给 `github-proxy` 加 `html` 动作（函数侧 fetch raw 再以 `text/html` 吐回，想让 iframe 同源）后，函数 URL 无论新旧 action 一律 30s 超时、函数日志里连请求记录都没有——函数运行时同样出不了网。**不要再用「函数代理 GitHub」这条路**，站内体验只能靠 storage 镜像 + 前端 srcDoc。
- ❌ Edge Function 中转大响应（实测 6.87MB HTML）→ 平台网关约 60s 直接 504（实测 60.2s / 60.7s / 120.8s 三种），且**断开后函数实例立即冻结**，无法后台续跑；函数内部先设体积阈值也无效（拉取本身就超时）。唯一可行方案：把自动镜像阈值收紧到 2MB（`MIRROR_MAX_BYTES`）并用 HEAD 预探体积直接跳过，超大文件交给站长在浏览器端直传（不受网关限制）。
- ❌ 浏览器直连 GitHub 彻底不可行：`raw.githubusercontent.com` 沙箱 TimeoutError；`api.github.com` Contents API 读 >1MB 报 `Failed to fetch`（会被 302 到 media 域）。不要试图用 github-proxy 的 blob action 兜底——实测拉 `app/index.html` 120.8s 后 504。
- ❌ **storage 对象读取时 content-type 恒被平台改写为 `text/plain;charset=UTF-8`**，即使上传时显式传 `text/html; charset=utf-8`。因此站内任何 HTML 都不能 iframe 直挂（会被当纯文本显示），必须前端 fetch 文本后交给 `iframe srcDoc`（**不要用 Blob URL**，见上条）。
- storage 对象存在性探测的正确 URL：`GET {SUPABASE_URL}/storage/v1/object/info/{BUCKET_ID}/{encPath}`（用 bucket name 会 400 `Bucket not found`）。批量删除：`DELETE {SUPABASE_URL}/storage/v1/object/{BUCKET_ID}` + body `{"prefixes":[...]}`，传 `{paths:[...]}` 或不传 body 都会 400。
- 函数侧 `SUPABASE_URL` / `SUPABASE_PUBLIC_URL` 都是私网地址，只能回桶内 path，公网 URL 由前端用自己的 `supabaseUrl` 拼。所有出网 fetch 必须带 `AbortSignal.timeout`，否则 GitHub 偶发挂起会把整个同步拖到网关超时。
- ❌ **生图产物要存「裸链接」不要存签名链接**：`meoo-cli image-generate` 的图落在平台 CDN（`https://g.cdn.meoo.host/<project>/ai-images/xxx.png`），带 `?auth_key=...` 的签名变体只是临时授权。实测**去掉 `?auth_key=` 的裸链接同样公开可访问（200 / image/png）且跨会话有效**，所以封面入库一律用裸链接；早期几张封面把带 `auth_key` 的签名地址写进了库，属于会过期的坑，已在本次统一替换。
- ❌ **`Uncaught AbortError` 的根因（勿回退）**：`AbortSignal.timeout()` 超时后 fetch 以 `DOMException: AbortError` 拒绝，这个异常**不是 `Error` 实例**——凡是 `e instanceof Error ? e.message : e` 这种写法都会把它降级成字符串 `"AbortError"`，既丢失「哪条来源超时」的上下文，又容易在未 await 的路径上冒泡成用户看不懂的未捕获异常。修法：**每个带 `AbortSignal.timeout` 的 fetch 就地 try/catch**，用 `e instanceof DOMException && e.name === "AbortError"` 判定并转成可读文案（如「仓库页面读取超时（6 秒），当前网络可能访问不到 GitHub」）；`fetchHtmlText()` 内部统一转换，调用方只需展示 `e.message`。**新增任何超时 fetch 都必须照此处理。**
- ❌ **「eggli-flashcard-app 展示是一片空白」的根因（勿回退）**：它是 **Vite 源码工程**（`vite.config.js` + `src/main.jsx`），仓库里**没有任何构建产物**；根 `index.html` 是开发态入口，整份只有 2015 字节的 `<div id="root"></div>` + `<script type="module" src="/src/main.jsx">`。旧的 `DEMO_NAMES` 把 `index.html` 当体验入口 → 2103 字节 < 2MB 被镜像进桶 → 前端 `srcDoc` 渲染时 `/src/main.jsx` 站内 404、浏览器也跑不了 JSX，于是纯白。**「文件名像入口」不等于「浏览器里跑得起来」**：体积小 ≠ 自包含，判断入口必须看它是否依赖仓库内其它文件（构建产物才是自包含的）。定位这类问题时 `cdn.jsdelivr.net`（`https://cdn.jsdelivr.net/gh/{owner}/{repo}@{ref}/{path}`，用 `Range: bytes=0-…` 取头部）在沙箱内可用，比 raw.githubusercontent.com 可靠。
- ⚠️ `CLOUD_VERT` 按 `-mv.z` 做纵深淡出（`smoothstep(6,46,d)` 起、`uRange*0.40~0.55` 收）。天穹层的云面只给小的正偏移（z = 2 / 6），偏移一大（>80）整层会被淡没，表现是「云凭空消失」。
